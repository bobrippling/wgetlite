-C: cookies (mm)

HTTP:
	Persistent connections?
		Like wget - reuse connection to server?
			struct conn
			{
				int sock;
				char *host;
			} *cons;
	Check Headers (Content-Disposition: attachment; filename="%s") for default filename (FIXME'd)
